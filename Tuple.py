## Tupple is similer to the list. but list  is mutable where tuple is immutable.

tup = (2,4,9,7,)
print(type(tup), tup)
print (tup[3])

num = (1,)  # when we have single element in tuple so we allways define with ","
print (type(num))


num = 1,
print (type(num))


#in tuple we can not perform any vale changeing methode. because tuple is immutable.